@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;700;900&family=Bangers&display=swap');

:root {
  --neon-green: #00ff62;
  --white-glow: #fff;
  --neon-cyan: #00ffff;
  --neon-pink: #ff00aa;
  --dark-bg: #0a0a1a;
  --electric-cyan: #00ffff;
  --hot-pink: #ff00aa;
}

/* Custom Fonts */
@font-face {
  font-family: 'aAnotherTag';
  src: url('../fonts/aAnotherTag.otf') format('opentype');
  font-weight: normal;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: 'PhillySans';
  src: url('../fonts/PhillySans.otf') format('opentype');
  font-weight: normal;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: 'aDrip1';
  src: url('../fonts/adrip1.ttf') format('truetype');
  font-weight: normal;
  font-style: normal;
  font-display: swap;
}

/* Font Classes */
.oracle-title { 
  font-family: 'aAnotherTag', 'Orbitron', monospace; 
}

.accent-text { 
  font-family: 'aDrip1', 'Bangers', monospace; 
}

.info-text { 
  font-family: 'PhillySans', 'Orbitron', monospace; 
}

/* Main Container */
.oracle-immersion-container {
  position: relative;
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  background: #000;
  font-family: 'PhillySans', 'Orbitron', monospace;
}

/* Graffiti Alley Background */
.alley-background {
  position: absolute;
  top: -15%;
  left: -15%;
  width: 130%;
  height: 130%;
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  will-change: transform;
  transition: opacity 1.2s ease, filter 1.2s ease;
  z-index: 1;
}

/* Updated positioning for UI controls */
.controls-container {
  position: absolute;
  bottom: 4%;
  left: 4%;
  z-index: 100;
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  width: 92%;
  padding: 0 2%;
}

.left-controls {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 15px;
}

.right-controls {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 15px;
  width: 92%; /* Take up more width to allow space-between */
  padding: 0 2%; /* Add some padding */
}

.left-controls {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 15px;
}

.right-controls {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 15px;
}

/* SNEAKAR Branding */
.sneakar-branding {
  position: absolute;
  top: 5%;
  left: 50%;
  transform: translateX(-50%);
  z-index: 100;
  text-align: center;
  width: 90%;
  max-width: 900px;
}

.sneakar-title {
  font-size: 5rem;
  color: var(--neon-cyan);
  text-shadow: 
    0 0 10px var(--neon-cyan), 
    0 0 20px var(--neon-cyan), 
    0 0 30px var(--neon-cyan),
    0 0 40px var(--neon-pink),
    0 0 60px var(--neon-pink);
  letter-spacing: 0.2em;
  margin: 0;
  animation: title-glow 4s infinite ease-in-out;
}

@keyframes title-glow {
  0%, 100% { 
    text-shadow: 
      0 0 10px var(--neon-cyan), 
      0 0 20px var(--neon-cyan), 
      0 0 30px var(--neon-cyan),
      0 0 40px var(--neon-pink),
      0 0 60px var(--neon-pink);
  }
  50% { 
    text-shadow: 
      0 0 15px var(--neon-cyan), 
      0 0 30px var(--neon-cyan), 
      0 0 45px var(--neon-cyan),
      0 0 60px var(--neon-pink),
      0 0 90px var(--neon-pink);
  }
}

.sneakar-subtitle {
  font-size: 1.6rem;
  color: var(--neon-green);
  text-shadow: 0 0 8px var(--neon-green), 0 0 16px var(--neon-green);
  margin: 15px 0 0 0;
  letter-spacing: 0.1em;
  transition: all 0.3s ease;
}

.sneakar-subtitle:hover {
  color: var(--neon-pink);
  text-shadow: 0 0 12px var(--neon-pink), 0 0 24px var(--neon-pink);
  transform: scale(1.05);
}

/* Oracle Image Container */
.oracle-image-container {
  position: absolute;
  top: 45%;
  left: 50%;
  width: 280px;
  height: 280px;
  transform: translate(-50%, -50%);
  background-size: contain;
  background-position: center;
  background-repeat: no-repeat;
  cursor: pointer;
  z-index: 90;
  transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  display: block;
  visibility: visible;
  opacity: 1;
  overflow: visible;
  border-radius: 5%;
  filter: drop-shadow(0 0 20px rgba(0, 255, 255, 0.8)) 
          drop-shadow(0 0 20px rgba(255, 0, 204, 0.3)) 
          brightness(1.2) contrast(1.3) saturate(1.1);
  will-change: transform, filter;
}

.oracle-image-container:hover {
  transform: translate(-50%, -50%) scale(1.08);
}

.connecting-overlay {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: transparent;
  color: var(--neon-cyan);
  padding: 0;
  border: none;
  text-align: center;
  z-index: 300;
  box-shadow: none;
}

.connecting-text {
  font-size: 1.1rem;
  letter-spacing: 0.1em;
  animation: pulse 1.5s infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 0.7; }
  50% { opacity: 1; }
}

/* XR ENCULTURATE Button */
.enculturate-btn.cult {
  min-width: 200px;
  padding: 15px 30px;
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 1.6rem;
}

.enculturate-btn.monitor-btn {
  position: absolute;
  top: 70%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-family: 'aAnotherTag', 'PhillySans', monospace !important;
  font-size: 0.9rem;
  padding: 8px 16px;
  min-width: 100px;
  background: linear-gradient(45deg, #00ffff, #7c3aed);
  border: 2px solid var(--neon-green);
  color: var(--white-glow);
  text-shadow: 0 0 8px var(--neon-green), 0 0 15px var(--neon-green);
  box-shadow: 0 0 15px rgba(0, 255, 255, 0.4), 0 0 25px rgba(0, 255, 98, 0.3);
  border-radius: 8px;
  cursor: pointer;
  z-index: 150;
  letter-spacing: 1px;
  transition: all 0.3s ease;
  animation: monitor-btn-glow 2s infinite ease-in-out;
  text-transform: uppercase;
  font-weight: bold;
}

.enculturate-btn.monitor-btn:hover {
  transform: translate(-50%, -50%) scale(1.05);
  box-shadow: 0 0 20px rgba(0, 255, 255, 0.5), 0 0 30px rgba(0, 255, 98, 0.4);
  text-shadow: 0 0 12px var(--neon-green), 0 0 20px var(--neon-green);
}

@keyframes monitor-btn-glow {
  0%, 100% { 
    box-shadow: 0 0 15px rgba(0, 255, 255, 0.4), 0 0 25px rgba(0, 255, 98, 0.3);
  }
  50% { 
    box-shadow: 0 0 20px rgba(0, 255, 255, 0.5), 0 0 30px rgba(0, 255, 98, 0.4);
  }
}

.learn2earn-tag {
  font-size: 2rem;
  color: var(--neon-green);
  text-shadow: 0 0 15px var(--neon-green), 0 0 30px var(--neon-green);
  transform: rotate(-8deg);
  cursor: pointer;
  letter-spacing: 0.08em;
  transition: all 0.3s ease;
  text-transform: uppercase;
  white-space: nowrap;
}

.learn2earn-tag:hover {
  transform: rotate(-5deg) scale(1.1);
  color: var(--neon-pink);
  text-shadow: 0 0 20px var(--neon-pink), 0 0 40px var(--neon-pink);
}

/* Microphone button animations */
@keyframes mic-pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); box-shadow: 0 0 30px rgba(0, 255, 65, 0.8); }
}

.mic-active {
  animation: mic-pulse 2s infinite ease-in-out;
}

.graff-punks-boombox {
  width: 140px;
  height: 90px;
  background-size: contain;
  background-position: center;
  background-repeat: no-repeat;
  cursor: pointer;
  border-radius: 10px;
  transition: all 0.4s ease;
  filter: brightness(1) saturate(1);
}

.graff-punks-boombox:hover {
  transform: scale(1.05);
  filter: brightness(1.2) saturate(1.2);
}

/* Oracle Mode Overlay */
.oracle-mode-overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.85);
  backdrop-filter: blur(8px);
  z-index: 1000;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.oracle-exit-btn {
  position: absolute;
  top: 25px;
  right: 25px;
  background: rgba(255, 0, 0, 0.8);
  border: 3px solid rgb(255, 255, 255);
  color: rgb(255, 255, 255);
  padding: 15px 25px;
  border-radius: 50%;
  cursor: pointer;
  z-index: 2001;
  display: flex;
  align-items: center;
  gap: 10px;
  transition: all 0.3s ease;
  width: 60px;
  height: 60px;
  justify-content: center;
  box-shadow: 0 0 30px rgba(255, 0, 0, 1);
}

.oracle-exit-btn:hover {
  background: rgba(255, 0, 0, 1);
  transform: scale(1.05);
  box-shadow: 0 0 50px rgba(255, 0, 0, 1);
}

.conversation-status {
  position: absolute;
  top: 25px;
  right: 25px;
  background: rgba(0, 255, 255, 0.3);
  border: 2px solid rgb(0, 255, 255);
  color: rgb(0, 255, 255);
  padding: 15px 25px;
  border-radius: 25px;
  z-index: 2001;
  display: flex;
  align-items: center;
  gap: 10px;
}

.stop-conversation-btn {
  background: rgba(255, 0, 0, 0.2);
  border: 1px solid #ff6666;
  color: #ff6666;
  padding: 4px 8px;
  border-radius: 8px;
  cursor: pointer;
  margin-left: 8px;
  transition: all 0.2s ease;
}

.stop-conversation-btn:hover {
  background: rgba(255, 0, 0, 0.4);
}

/* Oracle Video */
.oracle-video-container {
  position: relative;
  width: 160%;
  max-width: 1200px;
  aspect-ratio: 16/9;
  border-radius: 20px;
  overflow: hidden;
  border: 3px solid var(--neon-cyan);
  box-shadow: 
    0 0 30px rgba(0, 255, 255, 0.6),
    0 0 60px rgba(0, 255, 255, 0.3);
}

.oracle-video {
  width: 200%;
  height: 200%;
  object-fit: cover;
  filter: contrast(1.2) brightness(1.1) saturate(1.1);
}

.video-controls {
  position: absolute;
  bottom: 15px;
  right: 15px;
  display: flex;
  gap: 10px;
}

.video-control-btn {
  background: rgba(0, 0, 0, 0.7);
  border: 1px solid var(--neon-cyan);
  color: var(--neon-cyan);
  width: 45px;
  height: 45px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.3s ease;
}

.video-control-btn:hover {
  background: rgba(0, 255, 255, 0.2);
  transform: scale(1.1);
}

.start-conversation-btn {
  background: rgba(0, 255, 255, 0.2);
  border: 2px solid var(--neon-cyan);
  color: var(--neon-cyan);
  padding: 15px 30px;
  border-radius: 20px;
  cursor: pointer;
  margin-top: 30px;
  font-size: 1.2rem;
  display: flex;
  align-items: center;
  gap: 10px;
  transition: all 0.3s ease;
}

.start-conversation-btn:hover {
  background: rgba(0, 255, 255, 0.4);
  transform: scale(1.05);
}

.start-conversation-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  transform: scale(1);
}

/* Error Display */
.error-display {
  position: fixed;
  top: 20px;
  right: 20px;
  background: rgba(255, 0, 0, 0.2);
  border: 2px solid rgba(255, 0, 0, 0.5);
  color: #ff6666;
  padding: 12px 20px;
  border-radius: 10px;
  z-index: 3000;
  max-width: 300px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  animation: error-slide-in 0.3s ease;
}

@keyframes error-slide-in {
  from { 
    opacity: 0; 
    transform: translateX(100%); 
  }
  to { 
    opacity: 1; 
    transform: translateX(0); 
  }
}

.error-close-btn {
  background: none;
  border: none;
  color: #ff6666;
  cursor: pointer;
  padding: 2px;
  border-radius: 3px;
  transition: all 0.2s ease;
}

.error-close-btn:hover {
  background: rgba(255, 0, 0, 0.2);
  transform: scale(1.1);
}

/* ======================
   DEBUG PANEL
   ====================== */

/* Responsive Design */
@media (max-width: 900px) {
  .sneakar-title { 
    font-size: 3.5rem; 
  }
  .sneakar-subtitle { 
    font-size: 1.2rem; 
  }
  .oracle-image-container { 
    width: 220px; 
    height: 220px; 
  }
  .enculturate-btn {
    font-size: 1.4rem;
    padding: 15px 25px;
    min-width: 160px;
    top: 68%;
  }
  .learn2earn-tag {
    font-size: 1.8rem;
  }
  .graff-punks-boombox {
    width: 100px;
    height: 65px;
  }
}

@media (max-width: 600px) {
  .sneakar-title { 
    font-size: 2.5rem; 
  }
  .sneakar-subtitle { 
    font-size: 1rem; 
  }
  .oracle-image-container { 
    width: 180px; 
    height: 180px; 
  }
  .enculturate-btn {
    font-size: 1.2rem;
    padding: 12px 20px;
    min-width: 140px;
    top: 65%;
  }
  .learn2earn-tag {
    font-size: 1.5rem;
  }
  .graff-punks-boombox {
    width: 90px;
    height: 60px;
  }
  .oracle-video-container {
    width: 95%;
  }
}